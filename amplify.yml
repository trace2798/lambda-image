version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
      - npx drizzle-kit migrate:latest     
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    - node_modules/**/*
